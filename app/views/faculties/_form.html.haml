= form_with(model: faculty, local: true) do |form|
  - if faculty.errors.any?
    .error_explanation
      %h2= "#{pluralize(@organization.errors.count, "error")} prohibited this organization from being saved:"
      %ul
      - faculty.errors.full_messages.each do |message|
        %li= message

  .field
    = form.label :name, "Nazwa wydziału"
    = form.text_field :name, id: :faculty_name

  .actions
    = form.submit "Zapisz"
